# Generated by mimic. DO NOT EDIT.
apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app.kubernetes.io/component: api
    app.kubernetes.io/instance: observatorium
    app.kubernetes.io/name: observatorium-api
    app.kubernetes.io/part-of: observatorium
    app.kubernetes.io/version: latest
  name: observatorium-xyz
  namespace: observatorium
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/component: api
      app.kubernetes.io/instance: observatorium
      app.kubernetes.io/name: observatorium-api
      app.kubernetes.io/part-of: observatorium
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app.kubernetes.io/component: api
        app.kubernetes.io/instance: observatorium
        app.kubernetes.io/name: observatorium-api
        app.kubernetes.io/part-of: observatorium
        app.kubernetes.io/version: latest
      namespace: observatorium
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app.kubernetes.io/instance
                  operator: In
                  values:
                  - observatorium
                - key: app.kubernetes.io/name
                  operator: In
                  values:
                  - observatorium-api
              topologyKey: kubernetes.io/hostname
            weight: 100
      containers:
      - args:
        - --log.level=debug
        - --logs.read.endpoint=http://observatorium-xyz-loki-query-frontend-http.observatorium.svc.cluster.local:3100
        - --logs.rules.endpoint=http://observatorium-xyz-loki-ruler-http.observatorium.svc.cluster.local:3100
        - --logs.tail.endpoint=http://observatorium-xyz-loki-querier-http.observatorium.svc.cluster.local:3100
        - --logs.write.endpoint=http://observatorium-xyz-loki-distributor-http.observatorium.svc.cluster.local:3100
        - --metrics.read.endpoint=http://observatorium-xyz-thanos-query-frontend.observatorium.svc.cluster.local:9090
        - --metrics.rules.endpoint=http://observatorium-xyz-rules-objstore.observatorium.svc.cluster.local:8080
        - --metrics.write.endpoint=http://observatorium-xyz-thanos-receive.observatorium.svc.cluster.local:19291
        - --middleware.rate-limiter.grpc-address=observatorium-xyz-gubernator.observatorium.svc.cluster.local:8081
        - --rbac.config=/etc/observatorium/rbac/config.yaml
        - --tenants.config=/etc/observatorium/tenants/config.yaml
        - --traces.read.endpoint=http://observatorium-xyz-jaeger-query.observatorium.svc.cluster.local:16686/
        - --traces.write.endpoint=observatorium-xyz-otel-collector:4317
        image: quay.io/observatorium/api:latest
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 8
          httpGet:
            path: /live
            port: 8081
          periodSeconds: 30
          timeoutSeconds: 1
        name: observatorium-api
        ports:
        - containerPort: 8080
          name: http-public
          protocol: TCP
        - containerPort: 8081
          name: http-internal
          protocol: TCP
        - containerPort: 8090
          name: grpc
          protocol: TCP
        readinessProbe:
          failureThreshold: 20
          httpGet:
            path: /ready
            port: 8081
          periodSeconds: 5
        resources:
          limits:
            cpu: "3"
            memory: 3Gi
          requests:
            cpu: "2"
            memory: 2Gi
        terminationMessagePolicy: FallbackToLogsOnError
        volumeMounts:
        - mountPath: /etc/observatorium/rbac
          name: rbac-config
          readOnly: true
        - mountPath: /etc/observatorium/tenants
          name: tenants
          readOnly: true
      - args:
        - --start-dummy-sidecar
        image: docker.io/dummy:latest
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 10
          httpGet:
            path: /live
            port: 7081
            scheme: HTTP
          periodSeconds: 30
        name: dummy-sidecar
        ports:
        - containerPort: 7081
          name: dummy-metrics
        - containerPort: 7080
          name: dummy
        resources: {}
        terminationMessagePolicy: FallbackToLogsOnError
        volumeMounts:
        - mountPath: /etc/dummy-sidecar/config.yaml
          name: config
          readOnly: true
          subPath: config.yaml
      nodeSelector:
        kubernetes.io/os: linux
      serviceAccountName: observatorium-xyz
      terminationGracePeriodSeconds: 120
      volumes:
      - configMap:
          name: observatorium-xyz-rbac
        name: rbac-config
      - name: tenants
        secret:
          secretName: observatorium-xyz-tenants
      - configMap:
          name: dummy-sidecar-config-new
        name: config
status: {}
